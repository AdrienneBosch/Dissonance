<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Dissonance.UserControls.Title_Bar"
        xmlns:Buttons="clr-namespace:Dissonance.UserControls.Buttons" 
        xmlns:SettingsMenu="clr-namespace:Dissonance.UserControls.SettingsMenu" 
        x:Class="Dissonance.MainWindow"
        mc:Ignorable="d"
        Title="MainWindow" 
        Height="557" 
        Width="960"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="White" 
        BorderBrush="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <local:CustomTitleBar HorizontalAlignment="Stretch" Height="31" VerticalAlignment="Top"/>
        <Grid x:Name="BodyGrid" Background="{DynamicResource BackgroundBrush}" Grid.Row="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Grid x:Name="ThemeToggleButtonContainer" Margin="910,10,10,476"/>
            <SettingsMenu:SettingsMennu HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top"/>
        </Grid>
    </Grid>
</Window>
